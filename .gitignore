# atl
.atl/

# openspec
openspec/

# python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
.env
.env.*
!.env.example
.env.local
docker-compose.override.yml
*.pem
*.key
secrets/
.npmrc
.pypirc

#SDD
sdd/

#headroom
headroom_memory.db

# Compiled Tailwind output — built by the ``tailwind`` Docker stage
# and by ``npm run build:css`` in local dev. Never commit it.
app/static/css/style.css

# Local Node.js artifacts (the ``package-lock.json`` IS committed so
# the Docker build is reproducible).
node_modules/
