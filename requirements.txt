# AdjudicaUY — runtime dependencies
#
# Pinned to fixed versions for reproducible builds. All releases below were
# published well before the project bootstrap date (see git log).

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Templating
jinja2==3.1.5

# Persistence
sqlalchemy==2.0.50
alembic==1.14.0
psycopg2-binary==2.9.10

# Configuration
pydantic-settings==2.7.0

# Scraper (used by the worker service, imported from the shared venv)
httpx==0.27.2
lxml==5.3.0
